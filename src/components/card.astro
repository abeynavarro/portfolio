---
import '../styles/card.css'
const {image, title, link, classCard} = Astro.props;
---
<a class={classCard} href={link}>
<div >
        <img src={image} alt={title} />
         <h3>{title}</h3>
 </div>
</a>